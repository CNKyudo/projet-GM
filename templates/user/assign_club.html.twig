{% extends 'base.html.twig' %}

{% block title %}Attribuer un club
{% endblock %}

{% block body %}
	{% set defaultFieldClass = 'block w-full rounded-xl border border-gray-300 px-3 py-2 focus:border-kyudo-royal-blue focus:outline-none focus:ring-2 focus:ring-kyudo-royal-blue' %}
	{% set invalidFieldClass = 'block w-full rounded-xl border border-red-300 bg-red-50/40 px-3 py-2 focus:border-red-500 focus:outline-none focus:ring-2 focus:ring-red-500' %}

	<div class="h-screen-with-navbar bg-kyudo-club px-4 pb-10 pt-16 sm:px-6">
		<div class="mx-auto max-w-5xl rounded-[32px] bg-kyudo-white/95 p-6 shadow-2xl ring-1 ring-black/5 backdrop-blur sm:p-8 md:p-10">
			<div class="grid gap-6 lg:grid-cols-[1.3fr_0.9fr]">
				<section class="rounded-3xl border border-kyudo-gm-light-grey bg-kyudo-white p-6 shadow-sm">
					<h1 class="text-3xl font-semibold tracking-tight text-kyudo-gm-dark">Attribuer un club</h1>
					<p class="mt-3 max-w-2xl text-sm leading-6 text-kyudo-gm-grey">
						Associez un ou plusieurs clubs à
						<span class="font-semibold text-kyudo-gm-dark">{{ targetUser.email }}</span>
						selon son rôle dans chaque club.
					</p>

					<div class="mt-8 max-w-2xl space-y-5">
						{{ form_start(form) }}
						{% if form.vars.errors|length %}
							{% include 'partials/_alert.html.twig' with {
                                    type: 'error',
                                    title: 'Le rattachement ne peut pas être enregistré pour le moment.',
                                    errors: form.vars.errors,
                                    class: 'mb-5'
                                } only %}
						{% endif %}

						{# Président #}
						<div>
							<label for="{{ form.clubWhichImPresidentOf.vars.id }}" class="mb-1 block text-sm font-medium text-kyudo-gm-grey">
								{{ form.clubWhichImPresidentOf.vars.label }}
							</label>
							{{ form_widget(form.clubWhichImPresidentOf, {
                                    attr: {
                                        class: form.clubWhichImPresidentOf.vars.errors|length ? invalidFieldClass : defaultFieldClass
                                    }
                                }) }}
							{% if form.clubWhichImPresidentOf.vars.errors|length %}
								{% include 'partials/_alert.html.twig' with {
                                        type: 'error',
                                        label: 'Champ invalide',
                                        errors: form.clubWhichImPresidentOf.vars.errors,
                                        class: 'mt-3'
                                    } only %}
							{% endif %}
						</div>

						{# Gestionnaire matériel #}
						<div>
							<label for="{{ form.clubWhereImEquipmentManager.vars.id }}" class="mb-1 block text-sm font-medium text-kyudo-gm-grey">
								{{ form.clubWhereImEquipmentManager.vars.label }}
							</label>
							{{ form_widget(form.clubWhereImEquipmentManager, {
                                    attr: {
                                        class: form.clubWhereImEquipmentManager.vars.errors|length ? invalidFieldClass : defaultFieldClass
                                    }
                                }) }}
							{% if form.clubWhereImEquipmentManager.vars.errors|length %}
								{% include 'partials/_alert.html.twig' with {
                                        type: 'error',
                                        label: 'Champ invalide',
                                        errors: form.clubWhereImEquipmentManager.vars.errors,
                                        class: 'mt-3'
                                    } only %}
							{% endif %}
						</div>

						{# Membre (multi-sélection) #}
						<div>
							<label for="{{ form.memberOfClubs.vars.id }}" class="mb-1 block text-sm font-medium text-kyudo-gm-grey">
								{{ form.memberOfClubs.vars.label }}
							</label>
							{{ form_widget(form.memberOfClubs, {
                                    attr: {
                                        class: (form.memberOfClubs.vars.errors|length ? invalidFieldClass : defaultFieldClass) ~ ' min-h-[7rem]'
                                    }
                                }) }}
							<p class="mt-1 text-xs text-kyudo-gm-grey">Maintenez Ctrl (ou Cmd) pour sélectionner plusieurs clubs.</p>
							{% if form.memberOfClubs.vars.errors|length %}
								{% include 'partials/_alert.html.twig' with {
                                        type: 'error',
                                        label: 'Champ invalide',
                                        errors: form.memberOfClubs.vars.errors,
                                        class: 'mt-3'
                                    } only %}
							{% endif %}
						</div>

						<div class="flex flex-col gap-3 sm:flex-row">
							<button type="submit" class="btn btn-primary">Enregistrer</button>
							<a class="btn btn-secondary" data-history-back href="{{ path('user_index') }}">Retour à la liste</a>
						</div>
						{{ form_end(form) }}
					</div>
				</section>

				<aside class="rounded-3xl border border-kyudo-gm-light-grey bg-kyudo-gm-light-grey p-6 shadow-sm">
					<h2 class="text-xl font-semibold text-kyudo-gm-dark">Résumé utilisateur</h2>

					<div class="mt-4 rounded-2xl bg-kyudo-white p-4 shadow-sm">
						<p class="text-sm font-medium text-kyudo-gm-grey">Compte</p>
						<p class="mt-2 break-all text-base font-semibold text-kyudo-gm-dark">{{ targetUser.email }}</p>
						<p class="mt-1 text-sm text-kyudo-gm-grey">ID #{{ targetUser.id }}</p>
					</div>

					<div class="mt-4 rounded-2xl bg-kyudo-white p-4 shadow-sm">
						<p class="text-sm font-medium text-kyudo-gm-grey">Rôles</p>
						<div class="mt-3 flex flex-wrap gap-2">
							{% for roleLabel in roleLabels %}
								<span class="rounded-full bg-kyudo-washed-blue px-3 py-1 text-xs font-semibold text-white">{{ roleLabel }}</span>
							{% else %}
								<span class="text-sm text-kyudo-gm-grey">Aucun rôle</span>
							{% endfor %}
						</div>
					</div>

					<div class="mt-4 rounded-2xl bg-kyudo-white p-4 shadow-sm">
						<p class="text-sm font-medium text-kyudo-gm-grey">Club (président)</p>
						<p class="mt-2 text-base font-semibold text-kyudo-gm-dark">
							{{ targetUser.clubWhichImPresidentOf ? targetUser.clubWhichImPresidentOf.name : 'Aucun' }}
						</p>
					</div>

					<div class="mt-4 rounded-2xl bg-kyudo-white p-4 shadow-sm">
						<p class="text-sm font-medium text-kyudo-gm-grey">Club (gestionnaire matériel)</p>
						<p class="mt-2 text-base font-semibold text-kyudo-gm-dark">
							{{ targetUser.clubWhereImEquipmentManager ? targetUser.clubWhereImEquipmentManager.name : 'Aucun' }}
						</p>
					</div>

					<div class="mt-4 rounded-2xl bg-kyudo-white p-4 shadow-sm">
						<p class="text-sm font-medium text-kyudo-gm-grey">Clubs (membre)</p>
						{% if targetUser.memberOfClubs|length %}
							<ul class="mt-2 space-y-1">
								{% for club in targetUser.memberOfClubs %}
									<li class="text-sm font-semibold text-kyudo-gm-dark">{{ club.name }}</li>
								{% endfor %}
							</ul>
						{% else %}
							<p class="mt-2 text-sm text-kyudo-gm-dark">Aucun</p>
						{% endif %}
					</div>
				</aside>
			</div>
		</div>
	</div>
{% endblock %}
